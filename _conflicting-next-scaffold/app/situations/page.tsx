'use client'
import { useState } from 'react'
export default function SituationsPage(){const [title,setTitle]=useState('');return <main className="form-page"><a href="/" className="text-link">← Back to Kindly</a><div className="form-panel"><span className="eyebrow">PREPARE TOGETHER</span><h1>Make a plan for a new situation.</h1><p>Name the moment, then add what you already know helps.</p><label htmlFor="situation-title">Situation</label><input id="situation-title" value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. A busy grocery store"/><label htmlFor="situation-detail">What might help?</label><textarea id="situation-detail" placeholder="Add words, choices, or a quiet plan."/><button className="button coral" onClick={()=>{if(title.trim())localStorage.setItem('kindly-situation',title)}}>Save situation</button></div></main>}
