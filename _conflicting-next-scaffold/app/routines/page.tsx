'use client'
import { useState } from 'react'
export default function RoutinesPage(){const [title,setTitle]=useState('');const [saved,setSaved]=useState(false);return <main className="form-page"><a href="/" className="text-link">← Back to Kindly</a><div className="form-panel"><span className="eyebrow">ROUTINES</span><h1>Create a routine that feels familiar.</h1><p>Give the routine a name and add a gentle note for the moments around it.</p><label htmlFor="routine-title">Routine name</label><input id="routine-title" value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Getting ready for school"/><label htmlFor="routine-note">What helps?</label><textarea id="routine-note" placeholder="Add steps, sensory supports, or a reassuring phrase."/><button className="button coral" onClick={()=>{if(title.trim()){localStorage.setItem('kindly-routine',title);setSaved(true)}}}>{saved?'Routine saved':'Save routine'}</button></div></main>}
