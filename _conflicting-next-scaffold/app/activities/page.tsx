'use client'
import { useState } from 'react'
export default function ActivitiesPage(){const [title,setTitle]=useState('');return <main className="form-page"><a href="/" className="text-link">← Back to Kindly</a><div className="form-panel"><span className="eyebrow">NEW ACTIVITY</span><h1>Add something your child enjoys.</h1><p>Turn a suggested toolkit into a small, repeatable activity.</p><label htmlFor="activity-title">Activity name</label><input id="activity-title" value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Draw the feeling"/><label htmlFor="activity-detail">How do you do it?</label><textarea id="activity-detail" placeholder="Describe the activity in your own words."/><button className="button coral" onClick={()=>{if(title.trim())localStorage.setItem('kindly-activity',title)}}>Add activity</button></div></main>}
